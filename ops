{"author":{"id":"2462ded6f56967dde059994c39a506c79e39e5dfd10c5a07f07a23a5652a8353"},"ops":[{"type":1,"timestamp":1775219588,"nonce":"qMtxFIbnKHU8rL5oKZ9qKemorjE=","title":"Add OpenHands to the Docker image and configure env var passthrough for LLM access.","message":"- tests/test_launch.py::test_openhands_env_vars_forwarded\n  Assert: when LLM_API_KEY, LLM_MODEL, LLM_BASE_URL are set, they appear in the docker command\n- tests/test_overflow.py::test_docker_cmd_openhands_env_passthrough\n  Assert: LLM_* vars are in _PASSTHROUGH_ENV_VARS\n\n- Dockerfile (modify — add `pip install openhands` after existing pip installs)\n- host/docker_cmd.py (modify — add LLM_API_KEY, LLM_MODEL, LLM_BASE_URL to _PASSTHROUGH_ENV_VARS)\n- docker-entrypoint.sh (modify — add `mkdir -p \"$HOME/.openhands\"` for conversation persistence)\n\n1. In Dockerfile, after `RUN pip install --break-system-packages 'litellm[proxy]'`, add:\n   `RUN pip install --break-system-packages openhands`\n2. In `host/docker_cmd.py`, verify `_PASSTHROUGH_ENV_VARS` includes `LLM_API_KEY`, `LLM_MODEL`, `LLM_BASE_URL` (already present — verify only)\n3. In `docker-entrypoint.sh`, after the claude-auth copy block, add:\n   `mkdir -p \"$HOME/.openhands\" 2\u003e/dev/null || true`\n4. No Docker socket needed — OpenHands headless CLI mode runs without it\n\nSee existing pip install lines in Dockerfile.\nSee _PASSTHROUGH_ENV_VARS in host/docker_cmd.py for env var pattern.\n\nREQ: REQ-030\n\nDepends on: e7f291e (adapter skeleton, already merged)","files":null}]}